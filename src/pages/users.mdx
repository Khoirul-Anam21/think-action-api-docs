export const description =
  'On this page, we’ll dive into the different user endpoints you can use to manage users programmatically.'

# Users

As the name suggests, users are a core part of Protocol — Making new account. We'll look at how to query, create, update, and delete users. {{ className: 'lead' }}

## The user model

The user model contains all the information about your users, such as their username, email, role, and profile photo. It also able to edit account information.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the user.
  </Property>
  <Property name="username" type="string">
    The username for the user.
  </Property>
  <Property name="email" type="string">
    The email for the user.
  </Property>
  <Property name="password" type="string">
    The password of the user.
  </Property>
  <Property name="photo" type="string">
    The Url of the profile picture
  </Property>
  <Property name="role" type="string">
    The role of the user
  </Property>
    <Property name="job" type="string">
    The job of the user (only facilitator)
  </Property>
  <Property name="created_at" type="timestamp">
    Timestamp of when the user was created.
  </Property>
  <Property name="updated_at" type="timestamp">
    Timestamp of when the user was updated.
  </Property>
</Properties>


---


## Retrieve user data {{ tag: 'GET', label: '/v1/users/:id' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a user by providing their user id. 

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/users/WAz8eIbvDR60rouK">


    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.users.get('WAz8eIbvDR60rouK')
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "id": "WAz8eIbvDR60rouK",
      "username": "FrankMcCallister",
      "email": "someone@mail.com",
      "password": "xxxxx-xxx-x-xx-xx(encrypted)",
      "photo": null,
      "job": null,
      "role": "student"
      "created_at": "692233200",
      "updated_at": 692233200,
    }
    ```

  </Col>
</Row>

---

## Update user {{ tag: 'PUT', label: '/v1/users/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a user. You can update the photo and the username of the user.

    ### Optional attributes

    <Properties>
      <Property name="photo" type="string">
        The display profile picture.
      </Property>
      <Property name="username" type="string">
        The user  name in the user list. By default, this is just the username.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/v1/users/WAz8eIbvDR60rouK">


    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.users.update('WAz8eIbvDR60rouK', {
        photo: 'photo_file',
    })
    ```


    </CodeGroup>

    ```json {{ title: 'Response' }}
    // Empty response
    ```

  </Col>
</Row>

---

## Delete user account {{ tag: 'DELETE', label: '/v1/users/:id' }}

<Row>
  <Col>

    This endpoint allows you to delete users from your user list in Protocol. 

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="DELETE" label="/v1/users/WAz8eIbvDR60rouK">

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.users.delete('WAz8eIbvDR60rouK')
    ```
    </CodeGroup>

  </Col>
</Row>
