export const description =
  'On this page, we’ll dive into the different Category endpoints you can use to manage Categorys programmatically.'

# Categorys

Category for the resolution, embedded in user data. Max 7 per user

## The category models

The Category model contains all the information about the Categorys.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the Category.
  </Property>
  <Property name="category" type="string">
    Category name
  </Property>
</Properties>

---

## List all Categories {{ tag: 'GET', label: '/v1/categories' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your Categorys. This endpoint usually used for catalog. By default, a maximum of ten Categorys are shown per page.


  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/categories">

    ```js
      const response = await axios.get('/v1/categories');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    
    {
      "categories": [
        {
          "_id": "category123",
          "categories": "Finish reading a book",
        },
        {
          "id": "hSIhXBhNe8X1d8Et"
          // ...
        }
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 4,
        "totalDocument": 16
      }
    }
    ```

  </Col>
</Row>

---


## Create a Category {{ tag: 'POST', label: '/v1/categorys' }}

<Row>
  <Col>

    This endpoint allows you to add a Category. 

    ### Required attributes

<Properties>
  <Property name="id" type="string">
    Unique identifier for the Category.
  </Property>
  <Property name="category" type="string">
    Category name
  </Property>
</Properties>
    

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/v1/categories">

    ```js
      const response = await axios.post('/v1/categories', {
        category: 'Finance',
      });
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "id": "xgQQXg3hrtjh7AvZ",
      "acknowledged": true,
    }
    ```

  </Col>
</Row>
