export const description =
  'On this page, we’ll dive into the different Resolution endpoints you can use to manage Resolutions programmatically.'

# Resolutions

Resolution is the main product of this application. Resolution is the topic of the learning that contains many contents{{ className: 'lead' }}

## The resolution models

The Resolution model contains all the information about the Resolutions.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the Resolution.
  </Property>
  <Property name="user_id" type="string">
    Identifier of the user associated with the Resolution.
  </Property>
  <Property name="resolution" type="string">
    Text content of the Resolution.
  </Property>
  <Property name="imageUrl" type="string">
    URL of the image associated with the Resolution.
  </Property>
  <Property name="category_id" type="string">
    Identifier of the category to which the Resolution belongs.
  </Property>
  <Property name="shareType" type="string">
    Type of sharing for the Resolution (e.g., public, private).
  </Property>
  <Property name="completed" type="boolean">
    Indicates whether the Resolution is completed or not.
  </Property>
  <Property name="dueDate" type="string">
    Due date for the Resolution.
  </Property>
</Properties>

---

## List all resolution {{ tag: 'GET', label: '/v1/resolutions' }} 

<Row>
  <Col>

   Get all resolution owned by a user

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/resolutions">

    ```js
    const response = await axios.get('/v1/resolutions');

    ```

    </CodeGroup>

    <CodeGroup title="Response" tag="GET" label="/v1/resolutions">
    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "_id": "resolution123",
          "user_id": "user123",
          "resolution": "Finish reading a book",
          "images": ["https://example.com/image.jpg", ...],
          "category_id": "category123",
          "shareType": "public",
          "completed": false,
          "dueDate": "2023-05-31",
          "createdAt": "2023-04-20T10:00:00Z",
          "updatedAt": "2023-04-20T10:00:00Z"
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument":1
      }
    }
    ```
    </CodeGroup>

  </Col>
</Row>

---

## List a user resolutions by category {{ tag: 'GET', label: '/v1/resolutions/category/:id' }} 

<Row>
  <Col>

   Get resolution by every category, only one resolution per category

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/resolutions/category/:id">

    ```js
    const response = await axios.get('/v1/resolutions/category/:id');

    ```

    </CodeGroup>

    <CodeGroup title="Response" tag="GET" label="/v1/resolutions/category/:id">
    ```json {{ title: 'Response' }}
    {
      "data": [
        {
          "_id": "resolution123",
          "user_id": "user123",
          "resolution": "Finish reading a book",
          "images": ["https://example.com/image.jpg", ...],
          "category_id": "category123",
          "shareType": "public",
          "completed": false,
          "dueDate": "2023-05-31",
          "createdAt": "2023-04-20T10:00:00Z",
          "updatedAt": "2023-04-20T10:00:00Z"
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument":1
      }
    }
    ```
    </CodeGroup>

  </Col>
</Row>

---

## Create a Resolution {{ tag: 'POST', label: '/v1/resolutions' }}

<Row>
  <Col>

    This endpoint allows you to add a Resolution. 

    ### Required attributes

    <Properties>
      <Property name="user_id" type="string">
          Identifier of the user associated with the Resolution.
        </Property>
        <Property name="resolution" type="string">
          Text content of the Resolution.
        </Property>
        <Property name="imageUrl" type="array">
          URL of the images associated with the Resolution.
        </Property>
        <Property name="category_id" type="string">
          Identifier of the category to which the Resolution belongs.
        </Property>
        <Property name="shareType" type="string">
          Type of sharing for the Resolution (e.g., public, private).
        </Property>
        <Property name="dueDate" type="string">
          Due date for the Resolution.
        </Property>
    </Properties>
    

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/v1/resolutions">

    ```js
      const response = await axios.post('/v1/resolutions', {
        user_id: 'user123',
        resolution: 'Finish reading a book',
        imageUrl: ['<image file>', ...],
        category_id: 'category123',
        shareType: 'public',
        dueDate: '2023-05-31'
      });
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "id": "xgQQXg3hrtjh7AvZ",
      "acknowledged": true,
    }
    ```

  </Col>
</Row>

---

## Edit a Resolution {{ tag: 'PUT', label: '/v1/resolutions/:id' }}

<Row>
  <Col>

    This endpoint allows you to update a Resolution by providing the Resolution id. 

    ### Optional attributes

    <Properties>
      <Property name="resolution" type="string">
          Text content of the Resolution.
      </Property>
      <Property name="imageUrl" type="string">
          URL of the image associated with the Resolution.
      </Property>
     <Property name="category_id" type="string">
          Identifier of the category to which the Resolution belongs.
     </Property>
     <Property name="shareType" type="string">
          Type of sharing for the Resolution (e.g., public, private).
     </Property>
     <Property name="dueDate" type="string">
          Due date for the Resolution.
     </Property>
    </Properties>
    

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/v1/resolutions/xgQQXg3hrtjh7AvZ">

    ```js
    const response = await axios.put('/v1/resolutions/xgQQXg3hrtjh7AvZ', {
      resolution: 'Read 10 pages of a book',
    });
    ```

    </CodeGroup>

    <CodeGroup title="Response">

    ```json {{ title: '204' }}
    // empty response tess
    ```

    </CodeGroup>



  </Col>
</Row>

---

## Delete a Resolution {{ tag: 'DELETE', label: '/v1/resolutions/:id' }}

<Row>
  <Col>

    This endpoint allows you to delete a Resolution by providing the Resolution id. Deleting this Resolution will delete all the comments inside.

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="DELETE" label="/v1/resolutions/xgQQXg3hrtjh7AvZ">

    ```js
    const response = await axios.delete('/v1/resolutions/xgQQXg3hrtjh7AvZ');
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    // empty response
    ```

  </Col>
</Row>


