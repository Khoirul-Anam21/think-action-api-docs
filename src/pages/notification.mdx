export const description =
  'On this page, we’ll dive into the different notification endpoints you can use to manage notifications programmatically.'

# notification

notifications are information that user received after other user interact in certain way to the current user

## Goal notification model

The notification model contains all the information about notification

### Properties

<Properties>
  <Property name="_id" type="objectId">
    The unique identifier for the notification.
  </Property>
  <Property name="user_id" type="objectId">
    The unique identifier of the user receiving the notification.
  </Property>
  <Property name="userNotified_id" type="objectId">
    The unique identifier of the user who triggered the notification.
  </Property>
  <Property name="post_id" type="objectId">
    The unique identifier of the related post (if applicable).
  </Property>
    <Property name="postType" type="objectId">
    The unique identifier of the related post (if applicable).
  </Property>
    <Property name="read" type="boolean">
    Indicates whether this notification is already read or not.
  </Property>
  <Property name="message" type="string">
    The content of the notification message.
  </Property>
</Properties>




---

## Get all notification from user {{ tag: 'GET', label: '/v1/notifications' }}
 
<Row>
  <Col>

    This endpoint will inform you about the user goals notification monthly
    
  </Col>
  <Col sticky>    

    <CodeGroup title="Request" tag="GET" label="/v1/notifications">
    ```js
    const response = await axios.get('/v1/notifications');


    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
    "data": [
        {
            "_id": "64c7d18a699be725b4d1a6a0",
            "post_id": "64bf727c6b5593912e36853a",
            "postType": "goal",
            "user_id": "64bb4529fc44e4615d4e1001",
            "userNotified_id": "64bb4529fc44e4615d4e1001",
            "message": "Mohammad Khoirul Anam cheered you",
            "read": false,
            "createdAt": null,
            "updatedAt": null
        },
        ...
    ],
    "pagination": {
        "page": 1,
        "pageCount": 1,
        "pageSize": 999,
        "totalDocument": 5
    }
}
    ```

  </Col>
</Row>

## Read (open notification) all notification {{ tag: 'PATCH', label: '/v1/notifications' }}
 
<Row>
  <Col>

    This endpoint will change all notifications status to read.
    
  </Col>
  <Col sticky>    

    <CodeGroup title="Request" tag="PATCH" label="/v1/notifications">
    ```js
    const response = await axios.patch('/v1/notifications');


    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    // empty response
    ```

  </Col>
</Row>