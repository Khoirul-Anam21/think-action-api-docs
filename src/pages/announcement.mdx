export const description =
  'On this page, we’ll dive into the different announcement endpoints you can use to manage announcements programmatically.'

# announcements

announcements are where the facilitator announce all the important information to its student. rather than in the comment, announcements should have a special and noticeable place {{ className: 'lead' }}

## The announcement model

The announcement model contains all the information about your announcements.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the announcement.
  </Property>
  <Property name="user" type="object">
    Unique identifier of user announcemented first.
  </Property>
  <Property name="course_id" type="string">
    Unique identifier of the course this announcement belongs to.
  </Property>
  <Property name="description" type="string">
    The content of the announcement.
  </Property>
</Properties>

---

## Create new announcement  {{ tag: 'POST', label: '/v1/announcements' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a list of all your announcements.
    
    ### Required attributes

    <Properties>
      <Property name="user_id" type="string">
        Unique identifier of user announcemented first.
      </Property>
      <Property name="course_id" type="string">
        Unique identifier of the course this announcement belongs to.
      </Property>
      <Property name="description" type="string">
        The content of the announcement.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/v1/announcements">

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.announcements.list({user: 'dmqweqwe', course_id:'csDaSDF44aas', description: 'Pengumuman untuk siswa...'})
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
     {
      "id": "fadfGas45",
      "user": {
        "username": "Anam",
        "photo": "cdn.photo_cloud_storage.com/xxxxxxx-xxxx",
      },
      "description": "Happy friday, here is a new announcement..",
      "created_at": 6589659,
     }
    ```

  </Col>
</Row>


---

## List all announcements  {{ tag: 'GET', label: '/v1/announcements' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a list of all your announcements.
    
    ### Required attributes

    <Properties>
      <Property name="course_id" type="string">
        The course of announcement referred.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/announcements">

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.announcements.list({course_id:'csDaSDF44aas'})
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    [
     {
      "id": "fadfGas45",
      "user": {
        "username": "Anam",
        "photo": "cdn.photo_cloud_storage.com/xxxxxxx-xxxx",
      },
      "description": "Happy friday, here is a new announcement..",
      "created_at": 6589659,
     },
     {
      "id": "fadfGfwe5",
      "user": {
        "username": "Anam",
        "photo": "cdn.photo_cloud_storage.com/xxxxxxx-xxxx",
      },
      "description": "There will be a new update on...",
      "created_at": 6589659,
     }
    ]
    ```

  </Col>
</Row>



