export const description =
  'On this page, we’ll dive into the different attachment endpoints you can use to manage attachments programmatically.'

# Goal Completion

This Goal Completion contains the completion of the weekly goal

## Completion model

The attachment model contains all the information about the goal completion
### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the Entity.
  </Property>
  <Property name="user_id" type="string">
    Identifier of the User associated with the Entity.
  </Property>
  <Property name="goal_id" type="string">
    Identifier of the Goal associated with the Entity.
  </Property>
  <Property name="completion_id" type="string">
    Identifier of the completion to which the Entity belongs.
  </Property>
    <Property name="category_id" type="string">
    Identifier of the category of the completion
  </Property>
  <Property name="caption" type="string">
    Caption or description of the Entity.
  </Property>
  <Property name="images" type="array">
    images associated with the Entity.
  </Property>
  <Property name="completeResolution" type="boolean">
    Flag indicating whether the resolution is complete for the Entity.
  </Property>
</Properties>

---

## Create a completion {{ tag: 'POST', label: '/v1/completions' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a list of all specified role.  

    ### Required attributes

    <Properties>
      <Property name="user_id" type="string">
        Identifier of the User associated with the Entity.
      </Property>
      <Property name="goal_id" type="string">
        Identifier of the Goal associated with the Entity.
      </Property>
      <Property name="caption" type="string">
        Caption or description of the Entity.
      </Property>
      <Property name="images" type="array">
        images associated with the Entity.
      </Property>
      <Property name="isCompleteResolution" type="boolean">
        Flag indicating whether the resolution is complete for the Entity.
      </Property>
    </Properties>

        ### Optional attributes

    <Properties>
      <Property name="completeResolution" type="boolean">
        Flag indicating whether the resolution is complete for the Entity.
      </Property>
    </Properties>

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/v1/completions">

    ```js
      const response = await axios.post('/v1/completions', {
        user_id: 'user123',
        goal_id: 'goal123',
        category_id: 'category123',
        caption: 'Entity caption',
        images: ['https://example.com/image.jpg', ...],
        completeResolution: true
      });
    ```
    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "_id": "sd0asu9djs",
      "acknowledged": true
    }
    ```
  </Col>
</Row>

---

## Edit a completion {{ tag: 'PUT', label: '/v1/completions/:id' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a list of all specified role.

    ### Optional attributes

    <Properties>
      <Property name="user_id" type="string">
        Identifier of the User associated with the Entity.
      </Property>
      <Property name="goal_id" type="string">
        Identifier of the Goal associated with the Entity.
      </Property>
      <Property name="category_id" type="string">
        Identifier of the category to which the Entity belongs.
      </Property>
      <Property name="caption" type="string">
        Caption or description of the Entity.
      </Property>
      <Property name="images" type="array">
        URL or path of the image associated with the Entity.
      </Property>
      <Property name="completeResolution" type="boolean">
        Flag indicating whether the resolution is complete for the Entity.
      </Property>
    </Properties>  
  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/v1/completions/dfadfa">

    ```js
      const response = await axios.put('/v1/completions/entity123', {
        caption: 'Updated entity caption',
      });
    ```
    </CodeGroup>

    ```json {{ title: 'Response' }}
    // empty response
    ```
  </Col>
</Row>

---

## Delete a completion {{ tag: 'DELETE', label: '/v1/completions/:id' }}

<Row>
  <Col>

    This endpoint allows you to delete a completion goal

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="DELETE" label="/v1/completions/dfadfa">

    ```js
    const response = await axios.delete('/v1/completions/dfadfa');
    ```
    </CodeGroup>

    ```json {{ title: 'Response' }}
    // empty response
    ```
  </Col>
</Row>
