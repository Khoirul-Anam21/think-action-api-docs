export const description =
  'On this page, we’ll dive into the different goal endpoints you can use to manage goals programmatically.'

# goals

goal is the sub part of the resolution that contains the materials about the resolution. this is the part where users actually learn the resolution.

## The goal model

The goal model contains all the information about the goals.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the Goal.
  </Property>
  <Property name="resolution_id" type="string">
    Identifier of the Resolution associated with the Goal.
  </Property>
  <Property name="goal" type="string">
    Text goal of the Goal.
  </Property>
  <Property name="category_id" type="string">
    Identifier of the category to which the Goal belongs.
  </Property>
  <Property name="shareType" type="string">
    Type of sharing for the Goal (e.g., public, private).
  </Property>
  <Property name="imageUrl" type="string">
    URL of the image associated with the Goal.
  </Property>
  <Property name="dueDate" type="string">
    Due date for the Goal.
  </Property>
</Properties>


---

## Create a goal {{ tag: 'POST', label: '/v1/goals' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a detailed information about the goal. Using goal id as http params. And user must specify the resolution id in the body.

    ### Required attributes

    <Properties>
      <Property name="goal" type="string">
        Text goal of the Goal.
      </Property>
      <Property name="category_id" type="string">
        Identifier of the category to which the Goal belongs.
      </Property>
      <Property name="shareType" type="string">
        Type of sharing for the Goal (e.g., public, private).
      </Property>
      <Property name="images" type="array">
        images associated with the Goal.
      </Property>
      <Property name="dueDate" type="string">
        Due date for the Goal.
      </Property>
    </Properties>



  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="POST" label="/v1/goals">


    ```js
    const response = await axios.post('/v1/goals', {
      goalText: 'Complete Chapter 1',
      category_id: 'category123',
      shareType: 'public',
      imageUrl: 'https://example.com/image.jpg',
      dueDate: '2023-06-30'
    });
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "_id": "fda0djfad",
      "acknowledged": true
    }
    ```

  </Col>
</Row>

---

## List all goal by category {{ tag: 'GET', label: '/v1/goals/category/:id' }}

<Row>
  <Col>

    This endpoint allows you to delete a goal

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="GET" label="/v1/goals/category/:id">


    ```js
    const response = await axios.get('/v1/goals/category/09j0wjre0jreweadlf');
    ```

    </CodeGroup>
    ```json {{ title: 'Response' }}
    {
      "goals": [
        {
          "id": "goal123",
          "resolution_id": "resolution123",
          "goalText": "Complete Chapter 1",
          "category_id": "category123",
          "shareType": "public",
          "imageUrl": "https://example.com/image.jpg",
          "dueDate": "2023-06-30",
          "createdAt": "2023-04-25T12:34:56Z",
          "updatedAt": "2023-04-25T12:34:56Z"
        }
        {
          "id": "hSIhXBhNe8X1d8Et"
          // ...
        }
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 4,
        "totalDocument": 16
      }
    }
    ```

  </Col>
</Row>

---

## Update a goal {{ tag: 'POST', label: '/v1/goals/:id' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a detailed information about the goal. Using goal id as http params. And user must specify the resolution id in the body.

    ### Optional attributes

    <Properties>
      <Property name="goal" type="string">
        Text goal of the Goal.
      </Property>
      <Property name="category_id" type="string">
        Identifier of the category to which the Goal belongs.
      </Property>
      <Property name="shareType" type="string">
        Type of sharing for the Goal (e.g., public, private).
      </Property>
      <Property name="images" type="array">
        images associated with the Goal.
      </Property>
      <Property name="dueDate" type="string">
        Due date for the Goal.
      </Property>
    </Properties>



  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="PUT" label="/v1/goals/:id">


    ```js
    const response = await axios.put('/v1/goals/0je0f0jdfadf', {
      goalText: 'Complete Chapter 1',
      category_id: 'category123',
      shareType: 'public',
      imageUrl: 'https://example.com/image.jpg',
      dueDate: '2023-06-30'
    });
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    // empty response
    ```

  </Col>
</Row>

---

## Delete a goal  {{ tag: 'DELETE', label: '/v1/goals/:id' }}

<Row>
  <Col>

    This endpoint allows you to delete a goal

  </Col>
  <Col sticky>

    <CodeGroup title="Request" tag="DELETE" label="/v1/goals/sdfa8ah66">


    ```js
    const response = await axios.delete('/api/goals/goal123');
    ```

    </CodeGroup>
    ```json {{ title: 'Response' }}
    // empty response
    ```

  </Col>
</Row>


---


