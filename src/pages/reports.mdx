export const description =
  'On this page, we’ll dive into the different report endpoints you can use to manage reports programmatically.'

# reports

reports are how you share things in Protocol — they allow you to send all sorts of files to your contacts and groups. On this page, we'll dive into the different report endpoints you can use to manage reports programmatically. We'll look at how to query, upload, update, and delete reports. {{ className: 'lead' }}

## Student report model 

The report model contains all the information about the files you send to your contacts and groups, including the name, type, and size.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the report.
  </Property>
  <Property name="user_id" type="string">
    Unique identifier for the user associated with the report.
  </Property>
  <Property name="course_id" type="string">
    Unique identifier for the course associated with the report
  </Property>
  <Property name="progress" type="double">
    The percentage of course completion.
  </Property>
  <Property name="score" type="string">
    The score report of the course.
  </Property>
  <Property name="status" type="integer">
    The completion status of the course.
  </Property>
  <Property name="latest_update" type="timestamp">
    Timestamp of when the report was updated.
  </Property>
</Properties>

## Facilitator report model

The report model contains all the information about the files you send to your contacts and groups, including the name, type, and size.

### Properties

<Properties>
  <Property name="id" type="string">
    Unique identifier for the report.
  </Property>
  <Property name="user_id" type="string">
    Unique identifier for the message associated with the report.
  </Property>
  <Property name="total_course" type="string">
    Total courses the facilitator have
  </Property>
  <Property name="total_participant" type="string">
    Total partipants enrolled in the facilitator courses.
  </Property>
  <Property name="complete_participant" type="string">
    Total participants completed the facilitator courses
  </Property>
  <Property name="latest_update" type="timestamp">
    Timestamp of when the report was updated.
  </Property>
</Properties>



---

## Get participant report by id {{ tag: 'GET', label: '/v1/participant_reports' }}
 
<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your reports (in a conversation if a conversation id is provided). By default, a maximum of ten reports are shown per page.

  </Col>
  <Col sticky>    

    <CodeGroup title="Request" tag="GET" label="/v1/reports">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/participant_reports \
      -H "Authorization: Bearer {token}" \
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.reports.participantReports.get()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.reports.participantReports.get()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->reports->participantReports->get();
    ```

    </CodeGroup>

    ```json {{ title: 'Response' }}
    {
      "id": "xxxxx-xxx-xx"
      "courses_count": 6,
      "courses_info": [
        {
          "id": " frdg453er3"
          "title": "Belajar Machine learning",
          "assignments": [
            {
              "title": "Penerapan Machine Learning versi 1",
              "is_complete": true,
              "score": 0.6,
            },
            {
              "title": "Penerapan Machine Learning versi 2",
              "is_complete": false,
              "score": 0,
            }
          ]
        },
        {
          "id": "hSIhXBhNe8X1d8Et"
          // ...
        }
      ]
    }
    ```

  </Col>
</Row>

---

## Get facilitator reports {{ tag: 'GET', label: '/v1/facilitator_reports' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your reports (in a conversation if a conversation id is provided). By default, a maximum of ten reports are shown per page.
    
  </Col>
  <Col sticky>

<CodeGroup title="Request" tag="GET" label="/v1/reports">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/facilitator_reports \
      -H "Authorization: Bearer {token}" \
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.facilitatorReports.get()
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.facilitatorReports.get()
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->facilitatorReports->get();
    ```

    </CodeGroup>
    ```json {{ title: 'Response' }}
    {
      "id": "xxxxx-xxx-xx"
      "courses_owned": 6,
      "courses_info": [
        {
          "id": " frdg453er3"
          "title": "Belajar Machine learning",
          "students_count": 13,
          "assignments": [
            {
              "title": "Penerapan Machine Learning versi 1",
            },
            {
              "title": "Penerapan Machine Learning versi 2",
            }
          ]
        },
        {
          "id": "hSIhXBhNe8X1d8Et"
          // ...
        }
      ]
    }
    ```

  </Col>
</Row>

--- 

## Get facilitator report detail by id {{ tag: 'GET', label: '/v1/facilitator_reports/:id' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your reports (in a conversation if a conversation id is provided). By default, a maximum of ten reports are shown per page.

  </Col>
  <Col sticky>

<CodeGroup title="Request" tag="GET" label="/v1/facilitator_reports/sdf434td">

    ```bash {{ title: 'cURL' }}
    curl -G https://api.protocol.chat/v1/facilitator_reports/sdf434td \
      -H "Authorization: Bearer {token}" \
    ```

    ```js
    import ApiClient from '@example/protocol-api'

    const client = new ApiClient(token)

    await client.facilitatorReports.get("sdf434td")
    ```

    ```python
    from protocol_api import ApiClient

    client = ApiClient(token)

    client.facilitatorReports.get("sdf434td")
    ```

    ```php
    $client = new \Protocol\ApiClient($token);

    $client->facilitatorReports->get("sdf434td");
    ```

    </CodeGroup>
    ```json {{ title: 'Response' }}
    {
      "id": "xxxxx-xxx-xx",
      "title": "Penerapan Machine Learning versi 1",
      "course": "Belajar Machine Learning",
      "student_reports": [
        {
          "id": "q5eqre00rkqe",
          "name": "AnamWidodo Maharani",
          "photo": "cdn.cloud_storage.com/img/sdtwesdfafd.jpg",
          "is_submit": true,
          "score": 0.78,
        },
        {
          "id": "q5eqre00rkqe",
          "name": "Mega panjaitan wati",
          "photo": "cdn.cloud_storage.com/img/sdtwe45rsdfafd.jpg",
          "is_submit": true,
          "score": 0.90,
        },
        {
          "id": "q5eqre00rkqe",
          "name": "Buzzer Lightyear",
          "photo": "cdn.cloud_storage.com/img/sdtwQQ4esdfafd.jpg",
          "is_submit": false,
          "score": 0,
        }
      ]
    }
    ```

  </Col>
</Row>

